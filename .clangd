# clangd 配置文件
# 参考文档: https://clangd.llvm.org/config

CompileFlags:
  # 显式设置编译数据库路径
  CompilationDatabase: build/linux-release
  Add:
    - -std=c++17
    - -DMUPARSER_STATIC
    - -Wno-vla-extension
    - -fopenmp=libgomp  # 显式告知 clangd 使用 libgomp (GCC 的 OpenMP)
  Remove:
    # 移除 clang 不支持的 GCC 特定选项
    - -fdiagnostics-color=always

Diagnostics:
  # 启用 clang-tidy 检查
  ClangTidy:
    Add:
      - modernize-*
      - bugprone-*
      - performance-*
    Remove:
      - modernize-use-trailing-return-type
      - readability-magic-numbers
      - readability-identifier-length

  UnusedIncludes: Strict

InlayHints:
  Enabled: true
  ParameterNames: true
  DeducedTypes: true
  Designators: true

Hover:
  ShowAKA: true

Completion:
  AllScopes: true

---
If:
  PathMatch: .*/mfem/.*
Index:
  Background: Skip
